<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NeoIsisJob.Views.Workout.SelectedWorkoutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NeoIsisJob.Views.Workout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <StackPanel Spacing="10">

            <!-- Selected Workout Title -->
            <TextBlock Text="{Binding SelectedWorkout.Name}" 
                       FontSize="24" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>

            <!-- List of Complete Workouts -->
            <ListView ItemsSource="{Binding CompleteWorkouts}" Margin="0,10,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Padding="10" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="{Binding ExerciseId}" FontSize="18" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Sets}" FontSize="14" Foreground="Gray"/>
                                <TextBlock Text="{Binding RepsPerSet}" FontSize="14" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </Grid>

</Page>
